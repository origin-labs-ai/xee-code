---
name: Design System Generation Skill
description: Import, extract, normalize, or persist a reusable DESIGN.md from an existing design system, HTML artifact, screenshot, template, style reference, or generated interface.
---

# Design System Generation Skill

 `DESIGN.md`。**** skill；、、、。（ DESIGN.md ）：、、****。 `skills/design/design-system-reference.md` 。

## 

- **Import（）**： `DESIGN.md`/，。：、、、、****、、。（" DESIGN.md "、""）
- **Extraction（）**： HTML////CSS/style skill/。：、、、****、 CSS、、。（" HTML  DESIGN.md"、""）
- **Persistence（）**：。：、、、****。（" DESIGN.md"、""）

，。

##  skill 

、、、（///）、（ `design-system-reference.md`）。

---

## Workflow

### 1. 
 Import / Extraction / Persistence；。

---

### 2. （）

，。

：`、。`
：`，；，。`

：、、、、。

### 3. 

。：****（）、****（）、****（）、****（）。

：
```text
：//。
：， hover 。
： hero。
：。
```

### 4.  DESIGN.md

 `DESIGN.md` → /； → ； →  `design-system-reference.md` 。， design bible。

---

# DESIGN.md 

 9 。** + **（），、。

**1. Visual Theme & Atmosphere** — ：、、、、、、 2 。**，**——"///"，；「」，（//，"1970 "）「」，///，「」。，。

**2. Color Palette & Roles** — ****，、， accent，、：
```md
### Core Palette
- Background `#F7F3EA` — ，。
- Text Primary `#171717` — 。
- Text Muted `#73706A` — 、、。
- Accent `#D94F30` —  CTA、、。
- Border `rgba(23,23,23,0.14)` — 。
```

**3. Typography Rules** —  + ****（ 9–15 ， headline/display/body/label/caption ， small/medium/large），//、（： 1.9×  5×）、--。

**4. Layout & Elevation（）** — /、、、、（）。**"/"**：；（ /  / ，、）——，。

**5. Component & Shape（）** — ****：（ / ）（ 4px，""）。****：（///）、、、，****（primary/secondary、hover/pressed/active、//）——，。

**6. Interaction & Motion** — 、/hover/、 reduced-motion，。

**7. Responsive Behavior** — 、、、。

**8. Do's and Don'ts** — 。**： Don'ts （）； +  Do/Don't 。**
```text
Do：；；； accent ；/；。
Don't：// logo；；/ UI；/emoji；；/； hero 。
```

**9. Agent Prompt Guide** —  agent 、：
```md
：；//；accent ；、 bio；、；；。
```

---


## Lightweight Tokens

If useful, include a compact token section.

Use only when values are available or can be responsibly inferred.

Example:

```json
{
  "color": {
    "background": "#F7F3EA",
    "surface": "#FFFFFF",
    "textPrimary": "#171717",
    "textMuted": "#73706A",
    "accent": "#D94F30",
    "border": "rgba(23, 23, 23, 0.14)"
  },
  "radius": {
    "sm": "6px",
    "md": "12px",
    "lg": "24px"
  },
  "spacing": {
    "base": "8px",
    "section": "96px"
  },
  "typography": {
    "bodySize": "16px",
    "bodyLineHeight": "1.5",
    "labelSize": "11px"
  }
}
```

Do not include fake precision. If values are inferred, state that they are inferred.

---

## （）

「」。：
- **Extraction**： CSS  DESIGN.md、、、/。
- **Persistence**：、、、。
- **Import**：//+/；。

## Quality Rules

 `DESIGN.md`：、、、、vs、IP、。****：、 premium/modern/clean 、、、、 token。

## IP and Brand Safety

：、 logo/// UI、、。：、、。

## Chinese Typography Reference

/CJK、、、、 deck、 UI  HTML ， `horizontal-craft/chinese-typography.md`； HTML ：、、、、、、。

## Quality Gate

 `quality-gate.md`；，。

## Handoff

/// `DESIGN.md`  `skills/design/design-system-reference.md` ，。：//、 style skill/、、、。
