# Keyless replay combines Code Mode with the durable image store and an exact
# image-capable replay route. The scenario generates its tiny PNG inside the
# run_code program, then exercises read_image as a nested dispatch.
- id: base
  name: '@deepseek-ai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: llm-deepseek
        name: '@origin-ai/xhe-llm-deepseek'
        disabled: true
      - id: acp-agent
        name: '@origin-ai/xhe-acp-demo'
        config:
          provider: deepseek-official
          model: deepseek-v4-flash-vision-exp
          persistenceRoot: !!js process.env.XHE_SNAPSHOT_SESSIONS_ROOT ?? './.sessions'
          persistenceCompression: none
          workspaceContext:
            maxBytes: 65536
          tools:
            mode: code
          persona: |
            You are a coding assistant powered by the {{model}} model. Your working directory is {{cwd}}.

            Verify your work by running the code or tests. Keep answers brief and factual.
      - insert:
          - id: attachment-local
            name: '@origin-ai/xhe-attachment-local'
          - id: code-runtime
            name: '@origin-ai/xhe-code-runtime-worker-thread'
          - id: llm-replay
            name: '@origin-ai/xhe-llm-replay'
            config:
              providers:
                - id: deepseek-official
                  name: DeepSeek
                  models:
                    - id: deepseek-v4-flash
                      inputModalities: [text]
                    - id: deepseek-v4-pro
                      inputModalities: [text]
                    - id: deepseek-v4-flash-vision-exp
                      inputModalities: [text, image]
