# Add the self-referential Cordis tools without changing the base ACP tool
# presentation mode.
- id: base
  name: '@deepseek-ai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: cordis-host-runner
            name: '@deepseek-ai/cordis-host-runner'
          - id: tool-cordis
            name: '@origin-ai/xhe-tool-cordis'
