# Keyless assembled-request snapshot for native DeepSeek image offload. The
# local provider endpoint is supplied by the snapshot test; the real attachment
# store and ACP bridge carry two uploaded images into one model request.
- id: base
  name: '@deepseek-ai/cordis-plugin-include'
  config:
    path: ../../cordis.yml
    patches:
      - id: llm-deepseek
        name: '@origin-ai/cf-llm-deepseek'
        config:
          apiKeyEnv: XHE_SNAPSHOT_API_KEY
          baseURL: !!js process.env.XHE_SNAPSHOT_BASE_URL
          thinking: disabled
          maxRequestFilesBytes: 92
          imageOffloadByteQuantum: 1
          models:
            - id: deepseek-v4-flash-vision-exp
              contextWindow: 32768
              maxTokens: 1024
              inputModalities: [text, image]
      - id: acp-agent
        name: '@origin-ai/cf-acp-demo'
        config:
          provider: deepseek-official
          model: deepseek-v4-flash-vision-exp
          persistenceRoot: !!js process.env.XHE_SNAPSHOT_SESSIONS_ROOT ?? './.sessions'
          persistenceCompression: none
          workspaceContext:
            maxBytes: 65536
          persona: |
            You are a CodeFusion coding assistant powered by the {{model}} model. Your working directory is {{cwd}}.

            Keep answers brief and factual.
      - insert:
          - id: attachment-local
            name: '@origin-ai/cf-attachment-local'
