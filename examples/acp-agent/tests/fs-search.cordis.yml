# Minimal live counterpart for the glob-sampling snapshot composition.
- id: llm-deepseek
  name: '@origin-ai/cf-llm-deepseek'
  config:
    models:
      - id: deepseek-v4-pro

- id: subprocess
  name: '@origin-ai/cf-subprocess-local'

- id: bash
  name: '@origin-ai/cf-bash-local'

- id: acp-agent
  name: '@origin-ai/cf-acp-demo'
  config:
    provider: deepseek-official
    model: deepseek-v4-pro
    persistenceRoot: !!js process.env.XHE_SNAPSHOT_SESSIONS_ROOT ?? './.sessions'
    persistenceCompression: !!js "process.env.XHE_SNAPSHOT === undefined ? 'zstd' : 'none'"
    workspaceContext: false
    skills:
      enabled: false
    toolJobs: false
    goals: false
    persona: You are a concise snapshot agent working in {{cwd}}.

- id: tool-fs-search
  name: '@origin-ai/cf-tool-fs-search'
  config:
    sampleOverCapGlobResults: true
    globMaxResults: 4
