# Unattended coding-agent deployment for the bundled xhe-jsonrpc-agent runtime.
# stdout is reserved for JSON-RPC; do not add a console logger or terminal UI.

- id: sdk-jsonrpc-server
  name: '@origin-ai/cf-sdk-jsonrpc-server'
  config:
    maxTokensAsSuccess: !!js "process.env.XHE_MAX_TOKENS_AS_SUCCESS === undefined ? true : JSON.parse(process.env.XHE_MAX_TOKENS_AS_SUCCESS)"

# The DeepSeek adapter. Shipped default: full thinking at max effort on every
# request; exact-model resolution materializes request defaults before logging.
# The model arrives per session over JSON-RPC, so it is not pinned here.
- id: llm-deepseek
  name: '@origin-ai/cf-llm-deepseek'
  config:
    thinking: enabled
    reasoningEffort: max

# Managed child-process groups for the bash executor (spawn/kill/output plumbing).
- id: subprocess
  name: '@origin-ai/cf-subprocess-local'

- id: bash
  name: '@origin-ai/cf-bash-local'
  config:
    cwd: !!js process.env.XHE_CWD ?? process.cwd()
    timeoutMs: 60000

- id: agent-spine
  name: '@origin-ai/cf-agent-spine-demo'
  config:
    persona: !!js process.env.XHE_SYSTEM_PROMPT ?? 'You are a coding agent.'
    workspaceContext: false
    skills:
      enabled: false
    toolBash:
      enableRunInBackground: false
    toolJobs: false

- id: sessions
  name: '@origin-ai/cf-session-persistence-jsonl'
  config:
    root: !!js process.env.XHE_SESSION_ROOT ?? './.sessions'
    # Snapshot runs read the raw JSONL back; production keeps zstd frames.
    compression: !!js "process.env.XHE_SNAPSHOT === undefined ? 'zstd' : 'none'"

- id: session-checkpoints
  name: '@origin-ai/cf-session-checkpoint-policy'

- id: subagent
  name: '@origin-ai/cf-subagent'

- id: subagent-spawn-in-process
  name: '@origin-ai/cf-subagent-spawn-in-process'
  config:
    providerName: spawn

- id: tool-subagent
  name: '@origin-ai/cf-tool-subagent'
  config:
    provider: spawn
    toolName: subagent
    enableRunInBackground: false

- id: tool-todo
  name: '@origin-ai/cf-tool-todo'
  config:
    allowParallelInProgress: true

- id: fs-local
  name: '@origin-ai/cf-fs-local'
  config:
    cwd: !!js process.env.XHE_CWD ?? process.cwd()

- id: fs-observation-policy
  name: '@origin-ai/cf-fs-observation-policy'

- id: tool-fs
  name: '@origin-ai/cf-tool-fs'

- id: token-meter
  name: '@origin-ai/cf-token-meter'

- id: compaction-basic
  name: '@origin-ai/cf-compaction-basic'
  config:
    thresholdRatio: 0.8
    retainRatio: 0.16
    maxTokens: 8192
    compactionRetries: 1
