# Keyless real-Loader composition for workspace-instruction resume
# reconciliation. The test seeds one persisted baseline, changes AGENTS.md
# while the session is offline, then resumes through the public agent service.

- id: persistence
  name: '@origin-ai/xhe-session-persistence-jsonl'
  config:
    root: './.sessions'
    compression: none

- id: replay
  name: '@origin-ai/xhe-llm-replay'
  config:
    file: !!js process.env.XHE_SNAPSHOT_FILE
    overrideFile: !!js process.env.XHE_SNAPSHOT_OVERRIDE

- id: fs-local
  name: '@origin-ai/xhe-fs-local'
  config:
    cwd: !!js process.cwd()

- id: agent
  name: '@origin-ai/xhe-agent-spine-demo'
  config:
    agents: []
    workspaceContext:
      maxBytes: 65536
      dshHome: !!js process.cwd() + '/.dsh'
    skills:
      enabled: false
    toolBash: false
    toolJobs: false
    goals: false

# Await the persisted resume before the headless driver inspects root agents.
- id: resumed-agent
  name: './tests/fixtures/workspace-context-resume-agent.ts'
