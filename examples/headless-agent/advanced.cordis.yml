# Add Code Mode and Cordis tools to the headless spawn/workflow stack.
- id: base
  name: '@deepseek-ai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: agent-spine
        name: '@origin-ai/cf-agent-spine-demo'
        config:
          agents:
            - id: main
              provider: deepseek-official
              model: deepseek-v4-pro
              cwd: !!js process.cwd()
          workspaceContext:
            maxBytes: 65536
          tools:
            mode: both
          persona: |
            You are headless-agent, a CodeFusion coding assistant powered by the {{model}} model. Your working directory is {{cwd}}.

            Verify your work by running the code or tests. Keep answers brief and factual.
      - id: persistence
        name: '@origin-ai/cf-session-persistence-jsonl'
        config:
          root: './.sessions'
          compression: !!js "process.env.XHE_SNAPSHOT === undefined ? 'zstd' : 'none'"
      - insert:
          - id: code-runtime
            name: '@origin-ai/cf-code-runtime-worker-thread'
          - id: cordis-host-runner
            name: '@deepseek-ai/cordis-host-runner'
          - id: tool-cordis
            name: '@origin-ai/cf-tool-cordis'
